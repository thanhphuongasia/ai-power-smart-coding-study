# T-06: Block list, Cell, router, main + test luồng

- ID · tier · model: T-06 · T2 · crew-impl-standard (sonnet)
- Owned files (dưới mobile/): lib/features/exercise/block_list_screen.dart, lib/features/exercise/cell_screen.dart, lib/app/router.dart, lib/main.dart, test/app_flow_test.dart
- Forbidden: mọi file khác
- Acceptance criteria:
  1. router.dart: GoRouter đúng routes INTERFACES.md; main.dart: ProviderScope + MaterialApp.router(theme: buildAppTheme()), không banner debug.
  2. Block list: header tên exercise + TimerPill, breadcrumb, stepper B1..Bn (done/active/locked có chữ/icon), banner "Bạn đang làm dở Block n" + nút "Tiếp tục" → Cell block active; danh sách block: done hiện code đáp án (mono), active hiện prompt, locked mờ + "Mở khoá sau Block n". Chỉ block done/active chạm được.
  3. Cell: header tên block + TimerPill (đổi sang danger khi idle), "Block n/m" + progress; các block done phía trên thu gọn (code mono); prompt; CodeInput (initialCode = draft, knownIdentifiersFor, block.vocab; onChanged → saveDraft; onActivity → session.activity()); nút Run (PrimaryButton) → output console (ResultBadge + output mono, danger khi sai); pass → nút "Block tiếp" (hoặc "Xong bài" ở block cuối → về Block list); HintButton mở bottom sheet 3 mức (Mờ / Vừa / Rõ), hiện mức đã mở, nút "Gợi ý rõ hơn" gọi useHint, "Còn n lần"; viền/nhãn hint màu accent.
  4. Idle: phase idle → dialog "Vẫn đang làm chứ?" + "Không có thao tác trong 2 phút. Code của bạn đã được lưu." + đếm ngược, nút "Nghỉ" (pop về Block list) và "Tiếp tục" (resume); timedOut → đóng dialog, pop về Block list, SnackBar "Đã lưu tiến độ".
  5. app_flow_test: bắt đầu từ `/` (Home), chạm continue-card → Block list → "Tiếp tục" → Cell block 3 của split-chunks (tổng 2 chạm từ Home tới Block list + 1 tới Cell); gõ bằng CodeInput dùng gợi ý (`suggestion-...`) và phím ký hiệu để ra đáp án đúng của block 3 (`for i in range(0, len(text), size):`) → Run → thấy "Đúng" → quay lại Block list thấy block 4 active (không còn locked). Test thêm: gõ `chu` ở block 3 thấy `suggestion-chunks`.
- Validation: cd mobile && flutter analyze && flutter test
- Evidence: git status --porcelain -uall -- mobile/lib mobile/test
- Lưu ý từ T-03: flutter_code_editor chạy timer debounce 5s của history controller — widget test có CodeInput phải unmount rồi `pump(Duration(seconds: 6))` trước khi kết thúc (xem helper `_teardown` trong test/code_input_test.dart), nếu không fail "A Timer is still pending". IME gửi nhiều ký tự một lần có thể bỏ qua auto-close — test gõ từng ký tự bằng `tester.testTextInput.updateEditingValue` như code_input_test.dart.
- BẮT BUỘC (từ review T-04): Cell screen phải `ref.watch(sessionProvider(exerciseId))` (hoặc ref.listen) trong build suốt thời gian màn mở. Chỉ `ref.read(...notifier).start()` mà không watch → autoDispose huỷ Timer ngay microtask kế tiếp, im lặng: timer và idle popup không bao giờ chạy. Gọi start() sau frame đầu (addPostFrameCallback) hoặc trong initState của ConsumerStatefulWidget. Test luồng phải kiểm TimerPill tăng sau `tester.pump(Duration(seconds: 2))`.
- run(blockIndex, code) không bounds-check — chỉ gọi với index của block active.
