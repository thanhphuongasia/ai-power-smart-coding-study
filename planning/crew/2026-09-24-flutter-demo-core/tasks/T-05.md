# T-05: Màn browse — Home, Theme detail, Topic detail

- ID · tier · model: T-05 · T2 · crew-impl-standard (sonnet)
- Owned files (dưới mobile/): lib/features/browse/home_screen.dart, lib/features/browse/theme_detail_screen.dart, lib/features/browse/topic_detail_screen.dart
- Forbidden: mọi file khác
- Acceptance criteria:
  1. Home: tiêu đề "Luyện tập hôm nay"; nếu inProgressExerciseProvider != null → thẻ "Tiếp tục" (Key('continue-card')) hiện tên exercise + "Block n/m", chạm → `/exercise/<id>`; danh sách theme (tên, mô tả, tags, tiến độ block done/tổng từ progressProvider), chạm → `/theme/<id>`.
  2. Theme detail (`ThemeDetailScreen(themeId)`): header + mô tả + tags, danh sách topic (số exercise, thanh tiến độ), chạm → `/topic/<id>`.
  3. Topic detail (`TopicDetailScreen(topicId)`): breadcrumb "Theme › Topic", nhóm theo file (tên file font mono), mỗi exercise: trạng thái xong/đang dở/mới (chữ + icon, không chỉ màu), `functionName()`, "n/m block", chạm → `/exercise/<id>`.
  4. Chỉ dùng AppColors/AppText/widgets dùng chung; không Color(0x..); không emoji; accent không dùng ở đây.
- Validation: cd mobile && flutter analyze && flutter test
- Evidence: git status --porcelain -uall -- mobile/lib/features/browse
