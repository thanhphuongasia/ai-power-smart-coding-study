# Review T-02 — Cell: idle activity, layout editor bàn phím đóng, back _CompactHeader

## Bước 0 — diff thật

`git status --short` tại worktree cho thấy 9 file thay đổi + 1 dir mới (`planning/crew/2026-09-24-pr4-codex-fixes/`):

- `mobile/lib/features/browse/home_screen.dart` — **ngoài scope T-02** (thuộc T-04)
- `mobile/lib/features/exercise/cell_screen.dart` — **owned T-02**
- `mobile/lib/features/exercise/code_input.dart` — **ngoài scope T-02** (thuộc T-03)
- `mobile/lib/shared/widgets.dart` — **ngoài scope T-02** (thuộc T-01)
- `mobile/test/browse_screens_test.dart` — ngoài scope (T-04)
- `mobile/test/cell_layout_test.dart` — **owned T-02**
- `mobile/test/code_input_test.dart` — ngoài scope (T-03)
- `mobile/test/theme_test.dart` — ngoài scope (T-01)
- `mobile/test/cell_idle_test.dart` (untracked, mới) — **owned T-02**
- `planning/crew/_index.md` — orchestrator, ngoài scope

Đúng như prompt lưu ý: 4 task chạy song song, các file ngoài T-02 chỉ liệt kê, không đánh giá.
**T-02 không có scope drift** — 3 file owned (`cell_screen.dart`, `cell_layout_test.dart`,
`cell_idle_test.dart`) đều có diff, không có file lạ nào bị T-02 đụng vào.

## 1. Scope

Đúng phạm vi khai (cell_screen.dart, cell_layout_test.dart, cell_idle_test.dart). Không xoá
test/comment nào không liên quan. Comment thêm vào giải thích rõ lý do (ListView shrinkWrap,
công thức promptCap, padding s1) — tốt cho người đọc sau.

## 2. Correctness

**INV-01 (idle reset):**
- `_runCode` (cell_screen.dart:52) gọi `sessionNotifier.activity()` trước `run()`.
- `_showHintSheet` (cell_screen.dart:72) gọi `activity()` trước `showModalBottomSheet`.
- Nút "Gợi ý rõ hơn" (cell_screen.dart:136-139) gọi `activity()` trước `useHint`.
- Tự thử: cp file gốc ra `/private/tmp`, xoá 2 lệnh `activity()` (Run + mở hint sheet) bằng
  Python string-replace, chạy lại `flutter test test/cell_idle_test.dart` → **2/3 test đỏ đúng
  như kỳ vọng** (`Expected: SessionPhase.active, Actual: SessionPhase.idle`), test thứ 3 (không
  chạm gì → idle) vẫn xanh. Khôi phục bằng `cp` từ bản backup, `cmp` xác nhận khớp bit-for-bit,
  `git status --porcelain` chỉ còn đúng diff gốc của worker. Test thật sự chứng minh hành vi,
  không phải test giả (not-null).

**Dialog chặn double-activity:** `showDialog(barrierDismissible: false, ...)` (cell_screen.dart
:153-155) là modal chuẩn Flutter — khoá tương tác với Run/Hint bên dưới cho tới khi đóng. Cờ
`_idleDialogOpen` (dòng 34, set/reset ở 152/181/193/201) chặn mở dialog chồng dialog. Không có
đường nào gọi `activity()` trong lúc dialog đang mở (Run/Hint không bấm được khi bị che) — xác
nhận đúng ý (3) trong yêu cầu kiểm.

**Layout editor bàn phím đóng (AC đã sửa theo DEVIATIONS.md):**
- Công thức `(avail - 300).clamp(0.0, avail * 0.25)` (cell_screen.dart ~314-318): khi
  `avail < 300`, `avail - 300` âm → `clamp` với lower=0.0 trả về 0.0, KHÔNG throw (Dart
  `clamp(lower, upper)` chỉ lỗi khi `lower > upper`; ở đây `upper = avail*0.25 ≥ 0` luôn ≥
  `lower = 0.0` vì `avail ≥ 0` — không có trường hợp crash). Đã đọc code xác nhận, không chỉ suy
  luận.
- Chạy thật `flutter test test/cell_layout_test.dart`: 3 kích thước bàn phím đóng đều xanh, số đo
  in ra qua `print`:
  - 360x640: editor=251.25 / prompt=115.75
  - 390x844: editor=404.25 / prompt=166.75
  - 412x915: editor=457.5 / prompt=184.5
  Cả ba đều ≥200dp và editor > prompt, đúng AC đã điều chỉnh trong DEVIATIONS.md. Lưu ý: số đo
  thật của tôi lệch nhẹ so với số worker báo (204/85, 365/154, 438/178) — không sai lệch về kết
  luận (assertion vẫn PASS ở cả hai bộ số), nhưng số worker báo cáo trong PLAN không khớp số đo
  lại được — **Minor**, xem finding bên dưới.
- `ListView(shrinkWrap: true)` (Key `cell-prompt-scroll`, cell_screen.dart ~245): co theo nội
  dung khi constraints loose (nhánh bàn phím đóng, bọc trong `ConstrainedBox(maxHeight:
  promptCap)`), vẫn cuộn được vì vẫn là `ListView` — nếu nội dung > `promptCap` thì
  `ConstrainedBox` giới hạn chiều cao viewport và `ListView` tự cuộn bên trong, không tràn ra
  Column cha. Đã xác nhận qua test không overflow (`tester.takeException()` isNull) ở mọi case.

**INV-05 (nhánh bàn phím mở giữ nguyên):** 3 case bàn phím mở (inset=300) trong
`cell_layout_test.dart` (6 case tổng = 3 size × 2 inset) đều xanh trong lần chạy thật. Đọc diff:
nhánh `if (keyboardOpen) [...]` (cell_screen.dart ~301-304) không đổi cấu trúc — chỉ có 1 dòng
padding chung (progress bar, s2→s1, không branch theo keyboardOpen) áp dụng cho CẢ HAI nhánh
như nhau, và dòng padding Run/Hint row đổi từ ternary `keyboardOpen ? s1 : s2` thành hằng số
`s1` — với nhánh mở, giá trị thực tế KHÔNG đổi (trước là s1, sau vẫn s1). Đúng ý "giữ nguyên"
cho nhánh bàn phím mở.

**Back `_CompactHeader`:** `GestureDetector` → `IconButton(tooltip: 'Quay lại', ...)`
(cell_screen.dart:432-436) — đúng quyết định trong PLAN, IconButton mặc định Material có vùng
chạm 48×48 (Flutter tap-target constraint), tooltip đặt đúng chuỗi 'Quay lại' làm nhãn screen
reader.

## 3. Edge case & error handling

- `avail < 300` (máy rất nhỏ/landscape): đã xác nhận không crash (mục 2). Không có test case
  thực tế phủ `avail < 300` (3 size test đều đủ lớn để avail sau header/progress còn > 300) —
  đây là gap về test coverage cho edge case cực nhỏ, nhưng logic công thức đã tự chứng minh an
  toán bằng đọc code + không phải finding chặn (không quan sát được hành vi sai, chỉ là thiếu
  test case bao phủ).
- Prompt dài hơn `promptCap`: `ListView` cuộn, không tràn — đã xác nhận qua absence of overflow
  exception trong test thật (mọi case `tester.takeException()` isNull).
- Dialog đang mở + user cố bấm Run/Hint: bị chặn bởi modal barrier, không double-fire
  `activity()` — không gây lỗi logic (activity() vô hại kể cả gọi thừa, nhưng ở đây còn không
  gọi được vì bị chặn UI).

## 4. Regression

- `app_flow_test.dart` (T-06 cũ, luồng Home→Cell→Run→Block mở khoá qua widget mới
  `IconButton`/layout mới) chạy xanh trong lần test thật (không nằm trong owned T-02 nhưng dùng
  để kiểm regression theo yêu cầu mục (7)).
- Không đổi public API/chữ ký hàm nào (`_runCode`, `_showHintSheet` vẫn `void`, tham số không
  đổi).
- Key `code-input-border` mà T-02 phụ thuộc để đo (từ T-03, `code_input.dart:228`) đã tồn tại
  trong diff song song — đã grep xác nhận, không phải finding chặn nhưng là điểm phụ thuộc
  chéo giữa T-02 và T-03 cần lưu ý ở review mối nối.

## 5. Test

- `cell_idle_test.dart` (mới, 3 test): chứng minh được hành vi thật qua thử ĐỎ (mục 2) — không
  phải test rỗng.
- `cell_layout_test.dart` bổ sung: assertion `borderHeight >= 200.0` và `borderHeight >
  promptHeight`, có `reason` rõ ràng, chạy thật PASS trên cả 3 size.
- Không thấy assertion kiểu "not null" hời hợt trong phần diff của T-02.

## Validation — output thật

```
$ cd mobile && flutter analyze
No issues found! (ran in 0.8s)

$ flutter test test/cell_layout_test.dart test/cell_idle_test.dart test/app_flow_test.dart
...
00:01 +10: All tests passed!
```

(10 test = 6 cell_layout [3 size × 2 inset] + 3 cell_idle + 1 app_flow, tất cả PASS.)

## Findings

**Minor** — `planning/crew/2026-09-24-pr4-codex-fixes/PLAN.md` (dòng ghi trong prompt review,
không phải trong file PLAN chính thức mà tôi đọc được) báo số đo layout khác với số đo thật
chạy lại (204/85 vs 251.25/115.75 ở 360x640; 365/154 vs 404.25/166.75 ở 390x844; 438/178 vs
457.5/184.5 ở 412x915). Không ảnh hưởng assertion (cả hai bộ số đều PASS ≥200 và editor>prompt),
nhưng số báo cáo không khớp số đo lại được — nếu số đó được dùng làm bằng chứng cho user (ví dụ
trong PR description hay comment trả lời Codex), cần cập nhật bằng số đo thật ở trên.
**Cách sửa:** dùng số đo từ `flutter test` chạy thật (in qua `print` trong test) khi báo cáo,
không phải số ước tính lúc code.
**Test cần bổ sung:** không cần — đây là finding về báo cáo, không phải về code.

Không có Critical/Major.

## Phần KHÔNG xác minh được

- Không kiểm được visual thật (screenshot) của layout mới trên thiết bị/`flutter build web` —
  chỉ xác minh qua widget test (rect/size), đúng phạm vi công cụ có sẵn.
- Không kiểm test coverage cho `avail < 300` thực tế (không có size test nào rơi vào trường hợp
  này) — đã xác nhận an toàn bằng đọc code, không phải bằng test chạy qua trường hợp đó.
- Review mối nối giữa T-02 và T-03 (phụ thuộc key `code-input-border`) không thuộc phạm vi task
  T-02 — cần reviewer mối nối riêng theo `seam-invariants.md`, tôi chỉ xác nhận key tồn tại qua
  grep, không đánh giá code_input.dart.

## Checklist NFR — baseline

SEC-01: N/A — thay đổi chỉ ở UI layout Flutter cục bộ, không có dữ liệu truyền qua mạng
SEC-02: N/A — không chạm lưu trữ/persistence
SEC-03: PASS — grep diff không thấy secret/token/key nhạy cảm, chỉ có `const Key(...)` là Flutter widget key, không phải secret
SEC-04: N/A — input là code người dùng gõ vào editor cục bộ, không băng qua trust boundary server; không có validate boundary nào bị đổi trong diff này
SEC-05: N/A — không chạm authZ/permission
PERF-01: N/A — không có query DB
PERF-02: PASS — build()/layout đều tính toán đồng bộ nhẹ (LayoutBuilder, clamp), không có I/O chặn main thread, cell_screen.dart:314-318
PERF-03: N/A — không có network call, pagination, hay retry trong diff này
MNT-01: PASS — `promptScroll` định nghĩa một lần, dùng lại ở cả 2 nhánh (bàn phím mở/đóng), không lặp logic, cell_screen.dart ~245-260
MNT-02: N/A — không có ranh giới module mới, chỉ sửa widget nội bộ một screen
MNT-03: PASS — thêm biến thể "bàn phím đóng" không sửa if/else đã ổn định cho nhánh "bàn phím mở" (đã xác nhận ở mục INV-05), chỉ thêm nhánh mới, cell_screen.dart ~301-330

## Kết luận

**PASS với Minor** (1 finding Minor về số đo báo cáo, không chặn). Validation command đã chạy
thật, PASS toàn bộ (`flutter analyze` sạch, 10/10 test xanh bao gồm regression `app_flow_test`).
INV-01 chứng minh được bằng thử-đỏ thật, INV-05 xác nhận bằng test + đọc code. Không có
Critical/Major.
