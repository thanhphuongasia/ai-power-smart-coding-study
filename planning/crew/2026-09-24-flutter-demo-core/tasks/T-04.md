# T-04: Session controller — tiến trình block, timer, idle, hint

- ID · tier · model: T-04 · T2 · crew-impl-standard (sonnet)
- Owned files (dưới mobile/): lib/features/exercise/session_controller.dart, lib/features/exercise/session_providers.dart, test/session_controller_test.dart
- Forbidden: mọi file khác; pubspec.yaml
- Objective: state phiên làm bài theo INTERFACES.md (Riverpod 3.4.3 Notifier API).
- Acceptance criteria:
  1. progressProvider khởi tạo từ seedCompletedBlocks; markBlockPassed chỉ tăng khi đúng block đang active; block N+1 locked cho tới khi N done (blockStatusOf).
  2. Draft lưu mỗi lần saveDraft; rời màn (sessionProvider dispose) rồi vào lại vẫn còn draft và số hint (progress không autoDispose).
  3. useHint tối đa 3 mỗi block.
  4. SessionController.start() chạy Timer.periodic 1s đọc clockProvider; không hoạt động idleThreshold → phase idle, idleCountdownSeconds đếm 60→0; activity()/resume() về active và reset mốc idle; hết countdown → timedOut; elapsed ≥ maxSessionDuration → timedOut. stop()/dispose huỷ timer.
  5. run(): pass → markBlockPassed; trả RunResult của runBlock.
  6. knownIdentifiersFor(split-chunks, 2) chứa text, size, chunks; (…, 0) chỉ chứa params.
  7. Test dùng fakeAsync + clock giả, không sleep thật; phủ 1–6.
- Validation: cd mobile && flutter analyze && flutter test
- Evidence: git status --porcelain -uall -- mobile/lib/features/exercise mobile/test
